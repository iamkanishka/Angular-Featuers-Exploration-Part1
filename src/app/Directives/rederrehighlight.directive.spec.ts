import { RederrehighlightDirective } from './rederrehighlight.directive';

describe('RederrehighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RederrehighlightDirective();
    expect(directive).toBeTruthy();
  });
});
